using UnityEngine;
using System.Collections;

public class invertcolours : MonoBehaviour {
	//public GameObject Turtle;
	//public Sprite.Texture2D;
	//public static Color[] pixels = TurtleTex.GetPixels ();

	// Use this for initialization
	void Start () {
		/*for (int i = 0; i < pixels.Length; i++) {
			pixels[i]=Color.black- pixels[i];
		}
		TurtleTex.SetPixels(pixels);
		TurtleTex.Apply();*/
	}

	// Update is called once per frame
	void Update () {



		}
	}

